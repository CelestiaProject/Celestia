
#undef NDEBUG

#include <iostream>
#include <cassert>
#include <celutil/utf8.h>

int main()
{/*
    std::string s("ALF cośtam");
    utf8_string u8s("ALF cośtam");
    assert(u8s == s);
    assert(isSubstring(u8s, s));
    assert(isSubstring(s, u8s));

    std::string s2 = ReplaceGreekLetterAbbr(s);
    utf8_string u8s2 = ReplaceGreekLetterAbbr(u8s.cpp_str());
    assert(u8s2 == s2);
    assert(isSubstring(u8s2, s2));
    assert(isSubstring(s2, u8s2));
    std::cout << "Original std::string: " << s << ", modified std::string: " << s2 << std::endl;
    std::cout << "Original utf8_string: " << u8s << ", modified utf8_string: " << u8s2 << std::endl;

    u8s2 += "œ ble ble";
    std::cout << "Extended utf8_string: " << u8s2 << std::endl;
    assert(isSubstring(u8s2, s2));
    assert(!isSubstring(s2, u8s2));

    assert(isSubstring("Betelquese", "Bet", false));
    assert(isSubstring("Betelquese", "Bet", true));
    assert(isSubstring("Betelquese", "bet", true));

    std::cout << "Characted diff: " << 'A' - 'B' << std::endl;
    std::cout << "Characted diff: " << 'B' - 'A' << std::endl;

    utf8_string n1("ALF Cen");
    utf8_string n2("BET Cen");
    utf8_string n3("GAM Cen");
    std::cout << n1.compare(n2) << std::endl;
    std::cout << n2.compare(n1) << std::endl;
    assert(n1 != n2);
    assert(n1 != n3);
    assert(n2 != n3);
    assert(n1 < n2);
    assert(n1 < n3);
    assert(n2 > n1);
    assert(n3 > n1);
    assert(n1 <= n2);
    assert(n1 <= n3);
    assert(n2 >= n1);
    assert(n3 >= n1);*/
}
