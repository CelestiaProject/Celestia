include(TestCase)

test_case(hash celengine)
test_case(fs celengine)
test_case(arraymap)
test_case(packedrangeset)
test_case(packedarraymap)
test_case(astroobj celengine)
if(WIN32)
  test_case(winutil celutil)
endif()
